name: mcp-use HOL Guard contract

on:
  pull_request:
    paths:
      - 'integrations/mcp-use-hol-guard/**'
      - '.github/workflows/mcp-use-hol-guard.yml'
  push:
    branches: [main]
    paths:
      - 'integrations/mcp-use-hol-guard/**'
      - '.github/workflows/mcp-use-hol-guard.yml'

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
        with:
          python-version: '3.11'
          cache: pip
      - name: Install proof and test dependencies
        run: python -m pip install './integrations/mcp-use-hol-guard[test]'
      - name: Run installed mcp-use middleware contract
        run: python -m pytest -q integrations/mcp-use-hol-guard/tests
